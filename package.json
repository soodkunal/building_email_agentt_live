{
  "name": "email-reply-agent",
  "version": "1.0.0",
  "description": "Email reply agent built with React, FastAPI, and Gemini",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:server": "uvicorn backend.main:app --reload --port 8000",
    "db:seed": "python scripts/ingest_knowledge.py"
  }
}
